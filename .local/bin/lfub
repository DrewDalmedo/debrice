#!/bin/sh

# Historically this was a wrapper that set up a ueberzug daemon for lf image
# previews. DARBS renders previews as text with chafa (see ~/.config/lf/scope)
# so no environment is needed; this passthrough is kept so existing aliases
# and bindings that call lfub keep working.

exec lf "$@"
