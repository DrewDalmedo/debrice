#!/bin/sh
# Remove the ueberzug overlay if one is active; chafa fallback previews are
# plain text in the preview pane and need no cleanup.
if [ -n "$FIFO_UEBERZUG" ]; then
	printf '{"action": "remove", "identifier": "PREVIEW"}\n' > "$FIFO_UEBERZUG"
fi
